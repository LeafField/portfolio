import { Meta, StoryObj } from "@storybook/react";
import SkillMap from "./SkillMap";

const meta: Meta = {
  title: "skill/SkillMap",
  component: SkillMap,
};

export default meta;

type Story = StoryObj<typeof meta>;

export const SkillMapStory: Story = {};
